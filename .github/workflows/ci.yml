name: OmniGuard CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  lint-and-test-backend:
    name: Backend Lint & Test
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: OmniGuard__backend/server

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies (Root)
        run: npm ci

      - name: Run backend tests
        run: npm test -- --coverage --forceExit
        env:
          NODE_ENV: test
          JWT_SECRET: ci-test-secret-key-minimum-32-characters-long
          JWT_REFRESH_SECRET: ci-test-refresh-secret-minimum-32-characters
          FIREBASE_PROJECT_ID: ci-test-project
          FIREBASE_CLIENT_EMAIL: test@ci-test.iam.gserviceaccount.com
          FIREBASE_PRIVATE_KEY: "-----BEGIN PRIVATE KEY-----\ntest\n-----END PRIVATE KEY-----\n"
          GEMINI_API_KEY: ci-test-gemini-key

      - name: Upload coverage report
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: OmniGuard__backend/server/coverage/
          retention-days: 7

  build-frontend:
    name: Frontend Build Check
    runs-on: ubuntu-latest
    needs: lint-and-test-backend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies (Root)
        run: npm ci

      - name: Build frontend
        run: npm run build -w omniguard-frontend

  docker-build:
    name: Docker Build Check
    runs-on: ubuntu-latest
    needs: build-frontend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t omniguard-api:ci ./OmniGuard__backend/server
